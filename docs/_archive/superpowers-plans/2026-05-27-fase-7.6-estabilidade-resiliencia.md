# FASE 7.6 — Estabilidade & Resiliência Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir 6 bugs de estabilidade que aparecem com 100+ usuários — memory leaks em sessões longas, UI congelada em rede instável, 5 queries Supabase por geração de prompt sem cache, e race condition em monthly usage counter.

**Architecture:** Três camadas distintas: (1) content script TypeScript — cleanup de observers/listeners no unload; (2) FastAPI no VPS — asyncio.wait_for no route handler + cache em memória por user_id; (3) Supabase — TTL job no APScheduler existente.

**Tech Stack:** TypeScript (Chrome Extension MV3), Vitest, FastAPI/Python 3.11, asyncio, APScheduler, Supabase (postgREST client), Docker.

---

## Arquivos Modificados

| Arquivo | Tipo | O que muda |
|---------|------|-----------|
| `src/content/injectButton.ts` | Modificar | Exportar `disconnectInjector()` |
| `src/content/content.ts` | Modificar | Salvar ref do observer + beforeunload cleanup |
| `src/core/usageCounter.ts` | Modificar | `incrementMonthlyUsage()` read-modify-write atômico |
| `src/auth/sessionManager.ts` | Modificar | Comentário documentando design da chave |
| `/root/atenna-backend/main.py` | Modificar (VPS) | `asyncio.wait_for` na chamada `generate_prompts` |
| `/root/atenna-backend/routes/bff_auth.py` | Modificar (VPS) | Cache TTL 60s em `usage_summary` + invalidação em `ingest_dlp_event` + cleanup endpoint |
| `/root/atenna-backend/main.py` | Modificar (VPS) | Job APScheduler para cleanup diário de dlp_events |
| `src/core/__tests__/usageCounter.test.ts` | Modificar | Testes de race condition em incrementMonthlyUsage |
| `src/content/__tests__/injectButton.test.ts` | Criar | Testes de disconnectInjector |

---

## Task 1: disconnectInjector() + cleanup de observers no content.ts

**Problema real:** `content.ts` cria um `MutationObserver` dentro de `init()` mas nunca o desconecta. Se a aba for recarregada ou fechar, o observer fica ativo. Em sessões longas (30+ min no ChatGPT), SPA navigations acumulam `tryInject()` calls desnecessários. Além disso, `injectButton.ts` não expõe uma função de cleanup pública para o `content.ts` chamar no unload.

**Files:**
- Modify: `src/content/injectButton.ts` (adicionar `disconnectInjector()`)
- Modify: `src/content/content.ts` (salvar ref observer + beforeunload)
- Create: `src/content/__tests__/disconnectInjector.test.ts`

- [ ] **Step 1: Escrever o teste failing**

Criar `src/content/__tests__/disconnectInjector.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock chrome APIs
const mockStorage = { get: vi.fn((_, cb) => cb({})), set: vi.fn((_, cb) => cb?.()) };
vi.stubGlobal('chrome', {
  storage: { local: mockStorage },
  runtime: { id: 'test-ext-id', getURL: (p: string) => `chrome-extension://test/${p}` },
});

describe('disconnectInjector', () => {
  beforeEach(() => { vi.resetModules(); });

  it('exporta função disconnectInjector', async () => {
    const mod = await import('../injectButton');
    expect(typeof mod.disconnectInjector).toBe('function');
  });

  it('disconnectInjector() não lança se chamado sem injectButton ter rodado', async () => {
    const { disconnectInjector } = await import('../injectButton');
    expect(() => disconnectInjector()).not.toThrow();
  });
});
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

```powershell
npx vitest run src/content/__tests__/disconnectInjector.test.ts
```

Expected: FAIL — `disconnectInjector` not exported.

- [ ] **Step 3: Adicionar `disconnectInjector()` ao `injectButton.ts`**

Em `src/content/injectButton.ts`, após a linha `let cancelDlpTimers: (() => void) | undefined;` (linha ~24), nenhuma mudança necessária ali.

Adicionar logo antes da função `injectButton` (antes de `export function injectButton(...)`):

```typescript
export function disconnectInjector(): void {
  currentCleanup?.();
  currentCleanup = undefined;
}
```

A função `currentCleanup` (já existente no módulo) remove os listeners de scroll/resize/input e desconecta o `ResizeObserver`. Isso é tudo o que `injectButton.ts` precisa expor.

- [ ] **Step 4: Rodar o teste para confirmar que passa**

```powershell
npx vitest run src/content/__tests__/disconnectInjector.test.ts
```

Expected: PASS — 2 tests.

- [ ] **Step 5: Atualizar `content.ts` para salvar ref do observer e chamar cleanup no unload**

Em `src/content/content.ts`, localizar a função `init()` (linha ~78). Há um `const observer = new MutationObserver(...)` criado localmente. Modificar para:

```typescript
import { detectPlatform } from './detectInput';
import { injectButton, removeButton, disconnectInjector } from './injectButton';
import { toggleModal, openSettingsOverlay } from '../ui/modal';
import { getSession } from '../auth/sessionManager';
import { setStorageUser } from '../core/scopedStorage';
import { attachImageInterceptor } from '../dlp/imageInterceptor';

// Cached session state — avoids re-checking on every MutationObserver tick
let _isAuthenticated = false;
let _domObserver: MutationObserver | null = null;
```

E na função `init()`, substituir:

```typescript
// ANTES:
const observer = new MutationObserver(() => {
  if (_isAuthenticated) tryInject();
});
observer.observe(document.body, { childList: true, subtree: true });
```

por:

```typescript
// DEPOIS:
_domObserver = new MutationObserver(() => {
  if (_isAuthenticated) tryInject();
});
_domObserver.observe(document.body, { childList: true, subtree: true });
```

E no bloco `if (window === window.top)` (linha ~179), adicionar após `void init()`:

```typescript
window.addEventListener('beforeunload', () => {
  _domObserver?.disconnect();
  _domObserver = null;
  disconnectInjector();
});
```

A adição deve ficar assim (trecho completo do bloco):

```typescript
if (window === window.top) {
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
      void init();
      window.addEventListener('beforeunload', () => {
        _domObserver?.disconnect();
        _domObserver = null;
        disconnectInjector();
      });
    });
  } else {
    void init();
    window.addEventListener('beforeunload', () => {
      _domObserver?.disconnect();
      _domObserver = null;
      disconnectInjector();
    });
  }

  // SPA navigation: history.pushState does not fire popstate, so we patch it.
  const _origPushState = history.pushState.bind(history);
  history.pushState = function (...args) {
    _origPushState(...args);
    setTimeout(tryInject, 0);
  };
  window.addEventListener('popstate', () => setTimeout(tryInject, 0));
}
```

- [ ] **Step 6: Rodar suite completa**

```powershell
npx vitest run --pool=forks --poolOptions.forks.singleFork
```

Expected: todos os testes passando (mínimo 253 testes).

- [ ] **Step 7: Build e verificar sem erros TypeScript**

```powershell
npm run build
```

Expected: build limpo, sem erros de tipo.

- [ ] **Step 8: Commit**

```powershell
git add src/content/injectButton.ts src/content/content.ts src/content/__tests__/disconnectInjector.test.ts
git commit -m "fix(stability): disconnectInjector + MutationObserver cleanup no beforeunload"
```

---

## Task 2: asyncio.wait_for na rota /generate-prompts (circuit breaker geral)

**Problema real:** `main.py` linha 227 chama `result = await generate_prompts(...)` sem timeout geral. `generate_prompts` tenta OpenAI (timeout 15s × 1 tentativa) depois Gemini (timeout 10s × 3 tentativas = 30s). Pior caso total: 45s de worker uvicorn travado por request. Com 10 requests simultâneos travados, o servidor para de responder.

**Files:**
- Modify: `/root/atenna-backend/main.py` (VPS) — linhas ~227 e ~267

- [ ] **Step 1: Localizar as duas chamadas `generate_prompts` em main.py**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n 'await generate_prompts' /root/atenna-backend/main.py"
```

Expected: duas linhas — ~227 e ~267.

- [ ] **Step 2: Verificar que asyncio já está importado**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n '^import asyncio' /root/atenna-backend/main.py"
```

Se não aparecer nada, o `import asyncio` precisa ser adicionado no topo do arquivo.

- [ ] **Step 3: Adicionar import asyncio se necessário e wrap das chamadas**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat > /tmp/patch_generate.py << 'PYEOF'
import re

with open('/root/atenna-backend/main.py', 'r') as f:
    src = f.read()

# Garantir que asyncio está importado
if 'import asyncio' not in src:
    src = 'import asyncio\n' + src

# Wrap chamadas generate_prompts em wait_for
# Padrão: result = await generate_prompts(...)
src = re.sub(
    r'(result = )await generate_prompts\(([^)]+)\)',
    r'\1await asyncio.wait_for(generate_prompts(\2), timeout=40.0)',
    src
)

with open('/root/atenna-backend/main.py', 'w') as f:
    f.write(src)

print('Done. Changed lines:')
for i, line in enumerate(src.splitlines(), 1):
    if 'wait_for(generate_prompts' in line:
        print(f'  line {i}: {line.strip()}')
PYEOF
python3 /tmp/patch_generate.py"
```

- [ ] **Step 4: Adicionar tratamento de TimeoutError próximo às chamadas**

O FastAPI precisa retornar 504 se o timeout estourar. Localizar os blocos try/except em torno das linhas das chamadas:

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n 'wait_for\|TimeoutError\|504' /root/atenna-backend/main.py | head -20"
```

Se não houver `asyncio.TimeoutError` sendo tratado, adicionar após cada `await asyncio.wait_for(...)`:

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat > /tmp/patch_timeout.py << 'PYEOF'
with open('/root/atenna-backend/main.py', 'r') as f:
    lines = f.readlines()

out = []
i = 0
while i < len(lines):
    out.append(lines[i])
    # After the wait_for line, look for the try/except context
    # We need to insert except asyncio.TimeoutError before the general except
    if 'wait_for(generate_prompts' in lines[i]:
        # Check if next ~5 lines already have TimeoutError handling
        snippet = ''.join(lines[i:min(i+10, len(lines))])
        if 'TimeoutError' not in snippet:
            # Detect indentation
            indent = len(lines[i]) - len(lines[i].lstrip())
            ind = ' ' * indent
            # Insert a try/except wrapper around the wait_for line
            # Replace the current line with try: + wait_for + except
            out.pop()  # remove what we just added
            out.append(ind + 'try:\n')
            out.append(ind + '    ' + lines[i].lstrip())
            out.append(ind + 'except asyncio.TimeoutError:\n')
            out.append(ind + '    raise HTTPException(status_code=504, detail="upstream_timeout")\n')
    i += 1

with open('/root/atenna-backend/main.py', 'w') as f:
    f.writelines(out)
print('Timeout handling added')
PYEOF
python3 /tmp/patch_timeout.py"
```

- [ ] **Step 5: Verificar resultado final**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n -A3 'wait_for(generate_prompts' /root/atenna-backend/main.py"
```

Expected: ver `asyncio.wait_for(..., timeout=40.0)` e `except asyncio.TimeoutError:` com `raise HTTPException(status_code=504, ...)`.

- [ ] **Step 6: Rebuild e testar**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "cd /root/atenna-backend && docker compose up --build -d backend 2>&1 | tail -5"
```

```bash
# Aguardar ~30s e verificar saúde
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "curl -s http://localhost:8000/health"
```

Expected: `{"status":"ok"}` ou similar.

---

## Task 3: Cache TTL 60s em GET /auth/usage + invalidação em POST /dlp/event

**Problema real:** `GET /auth/usage` faz 5 queries separadas no Supabase por chamada. `syncUsageFromServer()` do cliente chama isso após cada geração de prompt. 100 usuários ativos = 500 queries extras por burst de geração.

**Files:**
- Modify: `/root/atenna-backend/routes/bff_auth.py` (VPS)

- [ ] **Step 1: Adicionar cache dict no topo de bff_auth.py**

Localizar a seção de imports e variáveis no início de `bff_auth.py`. Após os imports existentes, adicionar:

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat > /tmp/patch_cache.py << 'PYEOF'
import re

with open('/root/atenna-backend/routes/bff_auth.py', 'r') as f:
    src = f.read()

# Adicionar cache vars após os imports (antes do primeiro @router)
cache_code = '''
# ── Usage cache (single-worker, in-memory, TTL 60s) ──────────────────────────
import time as _time_cache
_usage_cache: dict = {}  # user_id -> (monotonic_ts, dict)
_USAGE_CACHE_TTL = 60  # seconds

def _get_usage_cache(user_id: str):
    entry = _usage_cache.get(user_id)
    if entry and (_time_cache.monotonic() - entry[0]) < _USAGE_CACHE_TTL:
        return entry[1]
    return None

def _set_usage_cache(user_id: str, data: dict) -> None:
    _usage_cache[user_id] = (_time_cache.monotonic(), data)

def _invalidate_usage_cache(user_id: str) -> None:
    _usage_cache.pop(user_id, None)

'''

# Inserir antes do primeiro @router
insert_pos = src.find('@router.')
if '# ── Usage cache' not in src:
    src = src[:insert_pos] + cache_code + src[insert_pos:]
    with open('/root/atenna-backend/routes/bff_auth.py', 'w') as f:
        f.write(src)
    print('Cache code added')
else:
    print('Cache already present')
PYEOF
python3 /tmp/patch_cache.py"
```

- [ ] **Step 2: Modificar o handler `usage_summary` para usar o cache**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat > /tmp/patch_usage_handler.py << 'PYEOF'
with open('/root/atenna-backend/routes/bff_auth.py', 'r') as f:
    src = f.read()

# Localizar o início do corpo do handler de usage_summary
# Após: user_id = session['user_id']
# Antes do try:
old = \"    user_id = session['user_id']\\n\\n    try:\"
new = \"    user_id = session['user_id']\\n\\n    cached = _get_usage_cache(user_id)\\n    if cached is not None:\\n        return cached\\n\\n    try:\"
src = src.replace(old, new, 1)

# Antes do return final, adicionar _set_usage_cache
# O return está no formato: return { 'today': today, ... }
# Encontrar a linha do return dentro do try block de usage_summary
import re
# Substituir o return block para salvar no cache
old_return = \"        return {\\n            'today': today,\\n            'monthly': monthly,\\n            'total': total,\\n            'protected_count': protected,\\n            'scans_total': scans_total,\\n        }\"
new_return = \"        result = {\\n            'today': today,\\n            'monthly': monthly,\\n            'total': total,\\n            'protected_count': protected,\\n            'scans_total': scans_total,\\n        }\\n        _set_usage_cache(user_id, result)\\n        return result\"
src = src.replace(old_return, new_return, 1)

with open('/root/atenna-backend/routes/bff_auth.py', 'w') as f:
    f.write(src)
print('Usage handler patched')
PYEOF
python3 /tmp/patch_usage_handler.py"
```

- [ ] **Step 3: Verificar patches do handler**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n 'cached\|_get_usage_cache\|_set_usage_cache' /root/atenna-backend/routes/bff_auth.py"
```

Expected: ver `cached = _get_usage_cache(user_id)`, `if cached is not None:`, `_set_usage_cache(user_id, result)`.

- [ ] **Step 4: Adicionar invalidação no ingest_dlp_event**

No handler `ingest_dlp_event` (após o `ingest_dlp_event` gravar na tabela com sucesso), adicionar `_invalidate_usage_cache(user_id)`:

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat > /tmp/patch_invalidate.py << 'PYEOF'
with open('/root/atenna-backend/routes/bff_auth.py', 'r') as f:
    src = f.read()

# Após .execute() no ingest_dlp_event, adicionar invalidação
old = \"        ).execute()\\n    except Exception as e:\\n        logger.warning(f'ingest_dlp_event failed\"
new = \"        ).execute()\\n        _invalidate_usage_cache(user_id)\\n    except Exception as e:\\n        logger.warning(f'ingest_dlp_event failed\"
src = src.replace(old, new, 1)

with open('/root/atenna-backend/routes/bff_auth.py', 'w') as f:
    f.write(src)
print('Cache invalidation added to ingest_dlp_event')
PYEOF
python3 /tmp/patch_invalidate.py"
```

- [ ] **Step 5: Verificar patch de invalidação**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n '_invalidate_usage_cache' /root/atenna-backend/routes/bff_auth.py"
```

Expected: linha dentro do handler `ingest_dlp_event`, após `.execute()`.

- [ ] **Step 6: Rebuild e smoke test do cache**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "cd /root/atenna-backend && docker compose up --build -d backend 2>&1 | tail -5"
```

```bash
# Aguardar startup (~30s) e verificar health
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "sleep 30 && curl -s http://localhost:8000/health"
```

Expected: `{"status":"ok"}`.

---

## Task 4: TTL de 90 dias em dlp_events (cleanup job no APScheduler)

**Problema real:** `dlp_events` cresce ~5000 rows/dia com 500 usuários Pro. Em 1 ano = ~1.8M rows. Supabase free tier tem 500MB de storage. Sem cleanup, o projeto pode ser pausado.

**Files:**
- Modify: `/root/atenna-backend/routes/bff_auth.py` (VPS) — adicionar endpoint interno de cleanup
- Modify: `/root/atenna-backend/main.py` (VPS) — adicionar job APScheduler

- [ ] **Step 1: Adicionar endpoint de cleanup em bff_auth.py**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat >> /root/atenna-backend/routes/bff_auth.py << 'PYEOF'


# ── DLP events cleanup (internal — called by APScheduler) ────────────────────

async def cleanup_old_dlp_events() -> dict:
    from datetime import datetime, timedelta, timezone
    cutoff = (datetime.now(timezone.utc) - timedelta(days=90)).isoformat()
    try:
        client = get_admin_client()
        result = client.table('dlp_events').delete().lt('created_at', cutoff).execute()
        deleted = len(result.data) if result.data else 0
        logger.info(f'cleanup_old_dlp_events: deleted {deleted} rows older than 90 days')

        # Warn if table is still large after cleanup
        count_result = client.table('dlp_events').select('id', count='exact').execute()
        total = count_result.count or 0
        if total > 500_000:
            logger.warning(f'dlp_events has {total} rows — consider upgrading Supabase plan or reducing TTL')

        return {'deleted': deleted, 'remaining': total}
    except Exception as e:
        logger.warning(f'cleanup_old_dlp_events failed: {e}')
        return {'deleted': 0, 'error': str(e)}
PYEOF
echo 'cleanup function appended'"
```

- [ ] **Step 2: Verificar que a função foi adicionada**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n 'cleanup_old_dlp_events' /root/atenna-backend/routes/bff_auth.py"
```

Expected: 1 definição da função.

- [ ] **Step 3: Adicionar o job ao APScheduler em main.py**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 "cat > /tmp/patch_scheduler.py << 'PYEOF'
with open('/root/atenna-backend/main.py', 'r') as f:
    src = f.read()

# Adicionar import da função de cleanup
if 'cleanup_old_dlp_events' not in src:
    # Encontrar a linha de import das rotas (onde lifecycle_emails é importado)
    import_line = 'from routes.lifecycle_emails import'
    insert = 'from routes.bff_auth import cleanup_old_dlp_events\n'
    src = src.replace(import_line, insert + import_line, 1)

# Adicionar job ao scheduler (após o último add_job existente)
old_job_line = 'scheduler.add_job(run_upsell,        "cron", hour=11, minute=0,  id="daily_upsell",         replace_existing=True)'
new_job_lines = old_job_line + '\n    scheduler.add_job(cleanup_old_dlp_events, "cron", hour=3, minute=0, id="daily_dlp_cleanup", replace_existing=True)'
src = src.replace(old_job_line, new_job_lines, 1)

with open('/root/atenna-backend/main.py', 'w') as f:
    f.write(src)
print('Scheduler job added')
PYEOF
python3 /tmp/patch_scheduler.py"
```

- [ ] **Step 4: Verificar patches**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "grep -n 'cleanup_old_dlp_events\|daily_dlp_cleanup' /root/atenna-backend/main.py"
```

Expected: import na linha ~15 + `add_job` com `id="daily_dlp_cleanup"`.

- [ ] **Step 5: Rebuild e verificar startup sem erros**

```bash
ssh -i "C:\Users\dgapc\.ssh\ATENNAPLUGIN-DEPLOY" root@157.90.246.156 \
  "cd /root/atenna-backend && docker compose up --build -d backend && sleep 30 && docker compose logs --tail=20 backend"
```

Expected: logs sem `ImportError` ou `SchedulerError`. Pode ver `Scheduler started` com os 5 jobs.

---

## Task 5: incrementMonthlyUsage() read-modify-write atômico

**Problema real:** `incrementMonthlyUsage()` faz dois passos separados — `await getMonthlyUsage()` (passo 1) e depois `chrome.storage.local.set(...)` (passo 2). Se duas abas chamam `incrementMonthlyUsage()` concorrentemente, ambas leem `count=0`, ambas calculam `next=1`, ambas escrevem `count=1`. O resultado correto seria `count=2`.

**Files:**
- Modify: `src/core/usageCounter.ts` (linhas 121-130)
- Modify: `src/core/usageCounter.test.ts` (adicionar teste de concorrência)

- [ ] **Step 1: Adicionar o teste de concorrência**

Localizar `src/core/usageCounter.test.ts` e adicionar ao fim do arquivo:

```typescript
describe('incrementMonthlyUsage — read-modify-write atômico', () => {
  it('duas chamadas concorrentes produzem count=2, não count=1', async () => {
    vi.resetModules();

    // Mock chrome.storage.local com estado compartilhado real
    let store: Record<string, unknown> = {};
    vi.stubGlobal('chrome', {
      storage: {
        local: {
          get: vi.fn((key: string, cb: (r: Record<string, unknown>) => void) => {
            cb({ [key]: store[key] });
          }),
          set: vi.fn((obj: Record<string, unknown>, cb?: () => void) => {
            Object.assign(store, obj);
            cb?.();
          }),
        },
      },
      runtime: { id: 'test-ext-id' },
    });

    const { incrementMonthlyUsage } = await import('../usageCounter');

    // Disparar duas chamadas sem await — simula duas abas
    const [a, b] = await Promise.all([
      incrementMonthlyUsage(),
      incrementMonthlyUsage(),
    ]);

    // A soma dos incrementos deve ser 2
    expect(a + b).toBe(3); // 1 + 2 = 3 (ou 2 + 1 = 3, dependendo da ordem)
    // E o valor final no storage deve ser 2
    const { getMonthlyUsage } = await import('../usageCounter');
    const final = await getMonthlyUsage();
    expect(final).toBe(2);
  });
});
```

> **Nota:** Esse teste vai **falhar** com a implementação atual (retornará 1 em vez de 2), o que é o comportamento esperado antes da correção.

- [ ] **Step 2: Rodar o teste para confirmar que falha**

```powershell
npx vitest run src/core/usageCounter.test.ts
```

Expected: FAIL — `expect(final).toBe(2)` recebe `1`.

- [ ] **Step 3: Corrigir `incrementMonthlyUsage()` para read-modify-write atômico**

Em `src/core/usageCounter.ts`, substituir as linhas 121-130 (a função `incrementMonthlyUsage`):

```typescript
// ANTES:
export async function incrementMonthlyUsage(): Promise<number> {
  const current = await getMonthlyUsage();
  const next = current + 1;
  const currentMonth = getCurrentMonth();
  return new Promise(resolve => {
    try {
      chrome.storage.local.set({ [sk(MONTHLY_KEY)]: { count: next, resetMonth: currentMonth } }, () => resolve(next));
    } catch { resolve(next); }
  });
}
```

```typescript
// DEPOIS:
export async function incrementMonthlyUsage(): Promise<number> {
  return new Promise(resolve => {
    try {
      const key = sk(MONTHLY_KEY);
      chrome.storage.local.get(key, r => {
        const currentMonth = getCurrentMonth();
        const data = r[key] as MonthlyData | undefined;
        const base = (data?.resetMonth === currentMonth) ? (data?.count ?? 0) : 0;
        const next = base + 1;
        chrome.storage.local.set({ [key]: { count: next, resetMonth: currentMonth } }, () => resolve(next));
      });
    } catch { resolve(1); }
  });
}
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

```powershell
npx vitest run src/core/usageCounter.test.ts
```

Expected: PASS — todos os testes passando incluindo o novo.

- [ ] **Step 5: Rodar suite completa**

```powershell
npx vitest run --pool=forks --poolOptions.forks.singleFork
```

Expected: 0 falhas.

- [ ] **Step 6: Commit**

```powershell
git add src/core/usageCounter.ts src/core/usageCounter.test.ts
git commit -m "fix(stability): incrementMonthlyUsage() read-modify-write atômico (previne race em concurrent tabs)"
```

---

## Task 6: Comentário de design no sessionManager + build final + CHANGELOG

**Problema real (B6 do audit):** `sessionManager.ts` usa `chrome.runtime.id` como base da chave PBKDF2. O código não documenta que (a) há um salt aleatório por device, nem (b) qual é a proteção real vs limitação. Revisores internos podem subestimar ou superestimar a proteção.

**Files:**
- Modify: `src/auth/sessionManager.ts` (adicionar comentário)

- [ ] **Step 1: Adicionar comentário de design**

Em `src/auth/sessionManager.ts`, localizar a função `getDerivedKey()` (linha ~13). Adicionar o comentário antes da linha `async function getDerivedKey()`:

```typescript
// Key derivation design:
// - Base material: chrome.runtime.id (public, but not guessable without knowing extension ID)
// - Salt: crypto.getRandomValues (16 bytes, stored per-device in chrome.storage.local)
// - Combined via PBKDF2 / 100k iterations → AES-GCM-256
//
// Protection model: the random salt makes the key non-derivable from runtime.id alone.
// An attacker needs BOTH the extension ID AND the salt from chrome.storage.local.
// chrome.storage.local is isolated per extension — other extensions/pages cannot read it.
// This defends against: devtools inspection, storage dump from Chrome profile backup.
// This does NOT defend against: malware with full Chrome profile filesystem access.
```

- [ ] **Step 2: Build final e verificar contagem de testes**

```powershell
npx vitest run --pool=forks --poolOptions.forks.singleFork 2>&1 | Select-Object -Last 8
```

Expected: `Tests: 253+ passed`, `Errors: 0`.

```powershell
npm run build 2>&1 | Select-Object -Last 5
```

Expected: build limpo + `[build] ✓ localhost removido do dist/manifest.json`.

- [ ] **Step 3: Atualizar CHANGELOG.md com entrada de v1.3.0**

Em `CHANGELOG.md`, adicionar nova seção após `## [1.2.1]`:

```markdown
## [1.3.0] — 2026-05-27 — FASE 7.6: Estabilidade & Resiliência

### Fixed

- **MutationObserver sem cleanup**: `content.ts` agora desconecta o `MutationObserver` e chama `disconnectInjector()` no evento `beforeunload` — elimina acúmulo progressivo de listeners em sessões longas no ChatGPT
- **incrementMonthlyUsage() race condition**: read-modify-write dentro do callback único de `chrome.storage.local.get` — duas abas concorrentes agora produzem count correto em vez de perder incrementos

### Performance

- **Cache TTL 60s em GET /auth/usage**: resultados de usage ficam em memória por 60s por user_id; invalidado automaticamente ao receber `POST /auth/dlp/event` — elimina 5 queries Supabase desnecessárias a cada geração de prompt em burst
- **asyncio.wait_for(generate_prompts, timeout=40s)**: cap geral na rota `/generate-prompts`; retorna 504 se OpenAI + Gemini em conjunto demorarem mais de 40s — evita workers uvicorn travados indefinidamente

### Maintenance

- **TTL 90 dias em dlp_events**: job diário às 03h UTC no APScheduler remove eventos com mais de 90 dias; aviso de log se tabela ultrapassar 500k rows
- **Documentação do sessionManager**: comentário inline explicando modelo de proteção da chave AES-GCM (salt aleatório por device, isolamento por extensão)
```

- [ ] **Step 4: Commit e push final**

```powershell
git add src/auth/sessionManager.ts CHANGELOG.md
git commit -m "docs(security): comentário de design no sessionManager + CHANGELOG v1.3.0 (FASE 7.6)"
git push
```

Expected: push bem-sucedido em `main`.

---

## Self-Review

### Spec Coverage

| Item do spec | Task | Coberto? |
|---|---|---|
| MutationObserver disconnect + guard | Task 1 | ✅ |
| disconnectInjector() export | Task 1 | ✅ |
| beforeunload cleanup em content.ts | Task 1 | ✅ |
| asyncio.wait_for em /generate-prompts | Task 2 | ✅ |
| Cache TTL 60s em GET /auth/usage | Task 3 | ✅ |
| Invalidar cache ao receber dlp/event | Task 3 | ✅ |
| TTL 90 dias em dlp_events | Task 4 | ✅ |
| Job APScheduler para cleanup | Task 4 | ✅ |
| incrementMonthlyUsage() atômico | Task 5 | ✅ |
| Documentar sessionManager design | Task 6 | ✅ |

### Observações

- **Item 2 ajustado:** O spec original assumia que Gemini não tinha timeout. A investigação revelou que `gemini_service.py` já usa `httpx.AsyncClient(timeout=10.0)`. O fix foi aplicado na camada superior (`main.py`), mais correto arquiteturalmente.
- **Item 1 ajustado:** O spec falava em adicionar `_activeObserver` em `injectButton.ts`, mas esse arquivo não cria MutationObserver. O observer real está em `content.ts`. O fix está na camada correta.
- Tasks 2-4 são no VPS — não têm testes Vitest locais. Verificação é via `docker compose logs` e curl.
